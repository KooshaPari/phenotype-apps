pr_details.jsonl

# Generated binaries / build outputs that must not be committed
dagctl
dagctl.go
**/coverage-final.json

# Rust build artifacts
/target

# Example FPL scripts (scaffold output)
/examples/fpl

# iOS / SwiftPM / Xcode build artifacts (must never be committed)
apps/ios/**/.build/
**/.build/
**/DerivedData/
*.xcframework/

# Local worktree directory
FocalPoint-wtrees/
.build
.claude/worktrees/

# HeliosApp (Bun monorepo) build artifacts — apps/helios-app/.gitignore
# is self-contained for the subdir; these guard against root-level scans
# catching generated files in any other post-absorbs.
**/node_modules/
**/*.tsbuildinfo
**/coverage/
