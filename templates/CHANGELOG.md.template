<!--
  CHANGELOG.md template — human-readable release notes.
  Format: Keep a Changelog 1.1.0 (https://keepachangelog.com/en/1.1.0/).
  Versioning: Semantic Versioning 2.0.0 (https://semver.org/spec/v2.0.0.html).
  T20.6 of v8 plan; completes the 5th governance doc in the release-ready meta-bundle
  (per AGENTS.md §Conventions: AGENTS.md + llms.txt + WORKLOG.md + CHANGELOG.md + LICENSE-MIT).
  Cross-references T20.2 SPEC.md.template, T20.5 WORKLOG.md.template (source data).
-->

# Changelog

All notable changes to `{{REPO_NAME}}` are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
### Changed
### Deprecated
### Removed
### Fixed
### Security

## [0.1.0] - {{YYYY-MM-DD}}

### Added
- Initial release of `{{REPO_NAME}}` — see `SPEC.md` § 1 (What) for the one-line purpose.
- `{{primary entry point}}` — {{one-line description; e.g. `init(name)`, `Cli::run`, `serve()`}}.
- `{{secondary entry point}}` — {{one-line description; e.g. guard, error type, helper}}.

<!--
  Template notes:
  - Section order is fixed by Keep a Changelog 1.1.0: Added → Changed → Deprecated → Removed → Fixed → Security.
  - `[Unreleased]` is always present; keep all 6 empty subsections (they earn their place as a checklist).
  - Entries under each section are reverse-chronological (newest first).
  - Source data: CHANGELOG entries are GENERATED from `WORKLOG.md` v2.1 rows where
    `Device` ∈ {`ci`, `heavy-runner`} (release-producing work per ADR-025 + ADR-030).
    Rows with `Device` ∈ {`macbook`, `subagent`} are EXCLUDED (planning + dispatched-worker
    output are not user-visible releases per ADR-023 device-fit gate).
  - Fleet reference: pheno-otel/CHANGELOG.md, KWatch/CHANGELOG.md, PhenoProject/CHANGELOG.md.
  - Update cadence: on every release PR. Bulk backfill = one-time migration
    (pheno-worklog-schema/migrate_v2_to_v2_1.py).
  - Length: target ≤ 1 page (≤ 50 lines). If you outgrow it, release cadence is too dense — bump SemVer.
-->
