# {{REPO_NAME}} — SPEC.md

> **Spec status:** `{{draft | implemented | aspirational}}` — this document reflects {{the current codebase | a forward-looking design}}.
> **Last audited:** `{{YYYY-MM-DD}}` against tree `{{commit-sha | tag}}`.
> **Substrate tier:** `{{pheno-*-lib | phenotype-*-sdk | phenotype-*-framework | federated-service}}` (per ADR-023 Rule 3).

---

## 1. What (1 paragraph)

{{REPO_NAME}} is a **{{one-sentence purpose}}** that **{{one-sentence differentiator}}**.
It is consumed by {{primary consumer persona}} to {{primary value delivered}}.

## 2. Why (1 paragraph)

{{Problem statement — 1-2 sentences.}}
{{Why existing alternatives fall short — 1-2 sentences.}}
{{Win condition — 1 sentence.}}

## 3. How (architecture, 1-3 sentences + optional ASCII diagram)

{{3-sentence architecture summary: layers, key components, data flow.}}

```
{{Optional ASCII diagram, ≤ 20 lines. Show boundaries + data flow, not internals.}}
```

## 4. Interface (API surface, ≤ 10 lines)

```{{rust | python | typescript | go | shell}}
{{Primary entry point(s) — public API only. Trait/struct signatures, top-level function, CLI command.}}
{{Link to llms.txt for full API.}}
```

## 5. Status (current state, ≤ 5 bullet rows)

| Component | Status | Notes |
|---|---|---|
| {{component 1}} | `{{shipped | scaffold | partial | blocked}}` | {{one-line note}} |
| {{component 2}} | `{{shipped | scaffold | partial | blocked}}` | {{one-line note}} |
| {{component 3}} | `{{shipped | scaffold | partial | blocked}}` | {{one-line note}} |
| CI / tests | `{{green | yellow | red}}` | {{one-line note — coverage %, test count, etc.}} |
| Observability | `{{wired | partial | absent}}` | {{one-line note — OTLP endpoint, dashboard}} |

## 6. Out of scope (explicit deferrals)

- {{Deferred item 1}} — defer rationale
- {{Deferred item 2}} — defer rationale
- {{Deferred item 3}} — defer rationale

## 7. References

- `README.md` — quickstart, when/when NOT, install
- `llms.txt` — full API reference
- `WORKLOG.md` — change history (per ADR-025 v2.1, includes `device:` field)
- `CHANGELOG.md` — release notes
- `LICENSE-MIT` / `LICENSE-APACHE` — license
- {{External ADR / spec}} — related decision

---

## Template usage notes

- **Length:** target ≤ 1 page (≤ 80 lines). If you cannot fit, your scope is too large — split the substrate.
- **Status discipline:** mark every component with one of `shipped | scaffold | partial | blocked`. No "TBD".
- **Tone:** declarative, present tense. "X is Y" not "X will be Y".
- **Update cadence:** re-audit every quarter OR on any major architectural change, whichever comes first. Mark "Last audited" date.
- **Per ADR-042 (Substrate quality bar):** this SPEC.md is element 1 of the 7-element bar. CI lint (`pheno-ci-templates/quality-bar.yml`) fails PRs missing it.
