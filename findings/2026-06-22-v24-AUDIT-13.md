# AUDIT-13 — Registry staleness and disposition index (governance)

**Date:** 2026-06-22
**Domain:** Governance (phenotype-registry substrate)
**Severity:** P2
**Method:** `ls phenotype-registry*/` + read recent worklogs.

## Finding

The phenotype-registry has 18+ archive variants in the cone (`phenotype-registry-archived-local-work`, `phenotype-registry-conft-settly-final`, `phenotype-registry-curation-data`, `phenotype-registry-dagctl-final`, `phenotype-registry-datakit-final`, `phenotype-registry-depguard-final`, `phenotype-registry-devhex-platformkit-final`, `phenotype-registry-drain-next`, `phenotype-registry-helioscope-final`, `phenotype-registry-httpora-final`, `phenotype-registry-phenoforge-final`, `phenotype-registry-phenoschema-final`, `phenotype-registry-preserve-wip`, `phenotype-registry-profila-final`, `phenotype-registry-utils-boundary`). Plus `phenotype-registry/` (active, if it exists in cone).

Per `AGENTS.md:233` (ADR-043 registry refresh cadence: bi-weekly), the last refresh is implicit per the worklogs directory. `AGENTS.md:233` says bi-weekly cadence, last refresh date not in this audit's scope.

## Impact

- The `-final` suffix on 13 registry clones is misleading per ADR-028 (hybrid-with-staging-repo policy) — they are NOT final, they are stage snapshots.
- The `phenotype-registry/` (canonical) is referenced in many ADRs (ADR-031, ADR-035, ADR-043) but is **not visible in the cone** for this branch.

## Recommendation

- Verify `phenotype-registry/` (canonical) presence in cone; if missing, the sparse-checkout cone excludes it.
- Rename `-final` clones to `-<date>-snapshot` for clarity. Out of scope for cycle 14.
- The registry cadence ADR-043 should add a "max clones before consolidation" gate (current: 13 clones, target: ≤3).

**Files cited:** 15 `phenotype-registry-*` paths enumerated above; `AGENTS.md:233` (ADR-043).