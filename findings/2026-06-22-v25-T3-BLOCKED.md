# v25-T3 BLOCKED — pheno-context crate structure mismatch

**Date:** 2026-06-22
**Track:** v25 cycle-15 T3 (L37 hot-reload)
**Status:** BLOCKED — plan-vs-reality mismatch
**Disposition:** Defer to v26 or v27 (or split into a separate bootstrap task)

## What was attempted

Subagent dispatched to implement `pheno-context/src/hot_reload.rs` (~250 LOC, with `Reloadable` trait, file watcher via `notify`, etc) per `plans/2026-06-22-v25-71-pillar-cycle-15-p1.md` line 45.

## Why it halted

Sub-step 1 failed: `pheno-context/src/lib.rs` does **not exist** on branch `chore/v25-71-pillar-cycle-15-p1-2026-06-22` of `/Users/kooshapari/CodeProjects/Phenotype/repos`.

Per ADR-094 (no-process-termination) and the task's "halt-on-failure" constraint, the subagent stopped before any modifications.

## Inventory of `pheno-context/` on the meta-repo branch

| Path | Status |
|---|---|
| `pheno-context/src/lib.rs` | **MISSING** |
| `pheno-context/Cargo.toml` | **MISSING** |
| `pheno-context/src/oidc.rs` | Tracked (HEAD) |
| `pheno-context/src/hot_reload.rs` | Untracked orphan (6,687 bytes) — incompatible design (SIGHUP-based, not `notify`-based) |
| `pheno-context/tests/lifecycle_test.rs` | Untracked orphan |
| `pheno-context/tests/proptest_smoke.rs` | Untracked orphan |
| `pheno-context/Cargo.lock` | Untracked orphan |

`git ls-tree -r HEAD --name-only | grep "^pheno-context/"` returns **exactly one file**: `pheno-context/src/oidc.rs`.

The crate is not buildable on this branch.

## Where the real pheno-context crate lives

`pheno-context` is a standalone repo checked out via worktree:

- `phenotype-apps-L39-wt/pheno-context/` — on branch `chore/L39-clap-ext-2026-06-22`, HEAD `e73535a535`
  - `phenotype-apps-L39-wt/pheno-context/Cargo.toml` — proper manifest (`name = "pheno-context"`, `version = "0.1.0"`, deps: `thiserror`, `http`)
  - `phenotype-apps-L39-wt/pheno-context/src/lib.rs` — proper API (`Context`, `ContextBuilder`, `ContextError` for **HTTP request headers**)

But the worktree's `Context` is **HTTP-header-based** (request_id, span_id, trace_id, user_id, org_id), not config/runtime-value-based. The requested `Reloadable` trait would not match its API surface.

## Three options (none trivial)

| Option | Description | Risk |
|---|---|---|
| **A** | Work in the worktree where the crate actually lives (`phenotype-apps-L39-wt/pheno-context/`) | API mismatch (HTTP vs runtime config); deviates from plan branch |
| **B** | Bootstrap `lib.rs` + `Cargo.toml` in meta-repo first, then implement hot-reload | Introduces a new top-level crate member; potential duplicate-crate issue with worktree's `phenotype-apps` workspace |
| **C** | Cancel T3 / defer to v26 or v27 | L37 stays at 2.5; cycle-15 target miss on 1 pillar |

## Disposition

**Option C — defer**. L37 (hot-reload) stays at 2.5 for this cycle. The implementation requires a foundational decision about WHERE `pheno-context` lives (meta-repo vs standalone repo) that is out of scope for cycle-15 P1 reduction.

The orphan `pheno-context/src/hot_reload.rs` (SIGHUP-based design) is preserved on disk for future review.

## v25 cycle-15 impact

- **Original target:** 5 pillars lifted 2.5→3.0 (L51/L46/L37/L26/L28) + 1 pillar 2.0→2.5 (L60) + 1 parallel (L42) = 7 tracks
- **Actual completion (2026-06-22):** T1 (L51) ✅ + T2 (L46) ✅ + T7 (L42) ✅ + T3 (L37) **BLOCKED** + T4/T5/T6 PENDING
- **L37 stays at 2.5** for this cycle

## L37 — recommended path forward

1. **ADR-095** (future): "pheno-context canonical location — meta-repo vs standalone repo"
2. Once decided, either:
   - **A-track:** port the worktree's HTTP-based `Context` to meta-repo, then layer hot-reload on top
   - **B-track:** declare meta-repo `pheno-context` as canonical (with `RuntimeContext` + `Reloadable`), deprecate standalone repo
3. T3 retry in v26 cycle-16 once L37 is unblocked

## Subagent demonstrated correct pattern

- ADR-094 respected (no process termination)
- "Halt-on-failure" constraint respected
- 3 viable options + 1 "show-me-first" option surfaced for orchestrator decision
- No speculative commits

## Refs

- ADR-094: `docs/adr/2026-06-22/ADR-094-no-process-termination.md`
- v25 plan: `plans/2026-06-22-v25-71-pillar-cycle-15-p1.md` line 45
- cycle-15 probe: `findings/2026-06-22-71-pillar-cycle-15-probe.md`
- finding (subagent halt): this file