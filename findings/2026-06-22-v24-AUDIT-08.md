# AUDIT-08 — Vault client SDK presence (L50 input)

**Date:** 2026-06-22
**Pillar:** L50 (Vault migration)
**Severity:** P1
**Method:** `grep -rE "vault|Vault|hashicorp" --include="*.toml" --include="*.rs" --include="*.go" --include="*.py" --include="*.ts"` (filtered out node_modules and `.semgrep-rules`).

## Finding

After filtering out `node_modules` and `.semgrep-rules/`, **zero vault client SDK references exist** in the cone. Matches in `Parpoura/docs/context/conv2/chunk_*.md` are historical chat transcripts and `Parpoura/TRACK_B_TREASURY_COMPLIANCE_SPEC.md` is a planning doc, not code.

No `vault` crate in any Cargo.toml. No `hashicorp/vault` Go module. No `hvac` Python dep. No `@hashicorp/vault` npm pkg.

## Impact

L50 PR-1 (vault client wrapper per `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:28`) must author the client from scratch. The natural canonical placement per ADR-023 is `pheno-secret-scan` (already exists at fleet root, named per `ls -d pheno-*`) OR a new `pheno-vault` substrate. Recommend **pheno-vault** (new crate, substrate-canonical) to keep `pheno-secret-scan` (a scanner, not a fetcher) cleanly bounded.

## Recommendation

- T4-PR1: create `pheno-vault` substrate crate with one of:
  - Rust: `vault-rs` (community crate) wrapped behind a `VaultPort` trait per ADR-038 (hexagonal ports).
  - Go: `hashicorp/vault/api` direct dep (OmniRoute already uses Go 1.26 per `go.mod:3`).
- T4-PR2 (parallel run): env→vault dual-read with shadow-compare logging.
- T4-PR3 (cutover): env-source removal; ADR-083 references all 3 PRs.

**Files cited:** `go.mod:3`; `Parpoura/TRACK_B_TREASURY_COMPLIANCE_SPEC.md`; `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:28`.