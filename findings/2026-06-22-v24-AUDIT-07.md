# AUDIT-07 — Plaintext secret leak scan (L50 input)

**Date:** 2026-06-22
**Pillar:** L50 (Vault migration, target 0→2)
**Severity:** P1 (zero baseline; vault cutover needs an inventory)
**Method:** `grep -rE "(password|secret|api_key|apikey|token)\s*=\s*['\"]"` over `*.toml`, `*.yml`, `*.yaml`, `*.json`, `*.env*` (depth-bounded).

## Finding

Zero plaintext secret assignments in fleet-root `*.toml` / `*.yml` / `*.yaml` / `*.json` / `*.env*` files (the file_list shows no `*.env` files in the cone).

The matches returned by grep are all **semgrep rule definitions** (YAML patterns, not real secrets):

- `pheno/forgecode-fork/.semgrep-rules/secrets-detection.yml`
- `pheno/pheno-cli/.semgrep-rules/secrets-detection.yml`
- `pheno/agileplus-agents/.semgrep-rules/secrets-detection.yml`
- `pheno/agileplus/.semgrep-rules/secrets-detection.yml`
- `pheno/phenotype-router-monitor/.semgrep-rules/secrets-detection.yml`
- `pheno/phenotype-governance/.semgrep-rules/secrets-detection.yml`
- `pheno/phenotype-infrakit/.semgrep-rules/secrets-detection.yml`
- `pheno/agileplus-mcp/.semgrep-rules/secrets-detection.yml`
- `thegent/.semgrep-rules/secrets-detection.yml`
- `HexaKit/forgecode-fork/.semgrep-rules/secrets-detection.yml`

These are detection patterns, not actual leaked secrets. **The fleet is clean** per the pattern match.

## Impact

L50 vault cutover can proceed against an empty plaintext baseline. ADR-083 ("Vault as canonical secret backend", cited in `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:62`) is unblocked.

## Recommendation

- Confirm with `trufflehog` (already configured at `trufflehog.yml`) that the empty baseline holds across git history, not just the working tree.
- The semgrep rules above should be re-exported to the fleet-root `pheno-secret-scan/` directory per ADR-023 substrate placement (currently scattered across 10 repos).

**Files cited:** 10 semgrep rule paths above; `trufflehog.yml` (fleet root); ADR-083 ref `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:62`.