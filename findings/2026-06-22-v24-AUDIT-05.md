# AUDIT-05 — Cache implementation inventory (L17)

**Date:** 2026-06-22
**Pillar:** L17 (Cache tuning, target 1→3, paired with L18)
**Severity:** P1 (config knobs to document per ADR-040)
**Method:** `grep -rE "(ConnectionPool|connection_pool|bb8|deadpool|moka|lru)"` over Rust/Python/Go.

## Finding

Cache code is concentrated in **helios-cli** with 5 cache-shaped artifacts:

- `helios-cli/crates/harness_cache/Cargo.toml` (dedicated cache crate)
- `helios-cli/crates/harness_cache/src/adapters/mod.rs` (adapter pattern)
- `helios-cli/harness/src/harness/cache_utils.py`
- `helios-cli/harness/src/harness/bounded_cache.py`
- `helios-cli/harness/tests/test_perf_integration.py`

No other fleet repo has a `bounded_cache` / `moka` / `lru` reference. `OmniRoute-b10`, `phenotype-router`, `phenotype-mcp-router` use external cache backends (Redis / NATS / in-process map per `OmniRoute-b10/go.mod` lines 11-15) but are not flagged here because LRU/in-process caching is the scope.

## Impact

T3 (cache tuning, L17+L18) config knobs to document per ADR-040 must come from `helios-cli/crates/harness_cache/Cargo.toml` only. The bench harness should be co-located with `helios-cli/harness/tests/test_perf_integration.py`.

## Recommendation

- Read `helios-cli/crates/harness_cache/Cargo.toml` deps for crate features (size, ttl, eviction policy knobs).
- Author config knob doc alongside bench harness for T3.
- Do not block on absent caches in other repos; their absence is a P2 not P1.

**Files cited:** 5 paths above; ADR-040 (test coverage gates per tier); `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:27`.