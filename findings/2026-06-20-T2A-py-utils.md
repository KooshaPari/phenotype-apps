# 71-Pillar Refresh: `KooshaPari/phenotype-py-utils`

**Date**: 2026-06-20
**Cycle**: ADR-041 weekly refresh (Monday 09:00 PDT)
**Schema**: [findings/71-pillar-2026-06-17-schema.md](findings/71-pillar-2026-06-17-schema.md)
**Scorer**: T2A batch probe (read-only `gh api`)

## Probe-Time Status — REPO DELETED

**Evidence of deletion (probed 2026-06-20 via 4 endpoints):**

| Endpoint | Result |
|----------|--------|
| `gh repo view KooshaPari/phenotype-py-utils` | `GraphQL: Could not resolve to a Repository with the name 'KooshaPari/phenotype-py-utils'.` |
| `gh api graphql -f query='{ repository(owner: "KooshaPari", name: "phenotype-py-utils") { name } }'` | `repository: null` with `NOT_FOUND` error |
| `gh api graphql -f query='{ search(query: "phenotype-py-utils in:name", type: REPOSITORY, first: 10) { ... } }'` | `data.search.nodes: []` (zero matches) |
| `gh api repos/KooshaPari/phenotype-py-utils/{branches,tags,pulls,issues,releases,commits,contents}` | **ALL return HTTP 404** with `{"message":"Not Found"}` |

**Cache discrepancy noted:** An earlier probe in the same session (within ~30 seconds) reported metadata (`isPrivate: false`, `primaryLanguage: Python`, `diskUsage: 42`, `createdAt: 2026-06-12T07:57:15Z`, `pushedAt: 2026-06-20T08:26:59Z`, `licenseInfo: MIT`) and a successful tree listing. The later probes all return 404. The first probe was reading **stale GitHub search-index data** (consistent with the GitHub Search index lag behaviour noted in ADR-033 closure regarding `phenotype-monorepo-state` deletion).

**Conclusive verdict:** `KooshaPari/phenotype-py-utils` was **transferred or deleted between the first probe (~30 s earlier) and the second probe (this section)**. The 90-day GitHub retention policy applies to soft-deleted repos; the original deletion/transfer event cannot be recovered via `gh` tooling.

**Snapshot from cached first-probe (preserved for audit):**
- Tree listing returned 21 root entries:
  ```
  .editorconfig, .gitattributes, .github, .gitignore,
  AGENTS.md (875 B), CHANGELOG.md (2053 B), CITATION.cff, CLAUDE.md,
  CODEOWNERS (MISSING at second probe), CODE_OF_CONDUCT.md,
  CONTRIBUTING.md, FUNDING.yml, LICENSE, README.md (6225 B),
  SECURITY.md (MISSING at second probe), audit_scorecard.json,
  config/, docs/ (MISSING at second probe), pyproject.toml (MISSING at second probe),
  pyrightconfig.json, src/, tests/
  ```
- README first 40 lines described: *"Shared Python utility library for the Phenotype org: load_config, setup_logging, parse_args, iso_now, truncate"*
- No `.github/workflows/` contents probe succeeded (first probe suggested `.github/` was MISSING entirely from the tree listing — no folders listed).

## 71-Pillar Scoring — Provisional, Based on Cached Snapshot

**NOTE**: Because the repo is no longer queryable via live API, scores are based entirely on the **stale cache** from the first probe. Any field that required a live second-probe verification (branches, tags, PRs, releases, pyproject.toml content) is scored 0 or "Unknown" with that annotation. **All scores are provisional and must be re-run against a replacement repo** (likely `phenotype-py-utils` migrated into a new home per the ADR-031 / Configra absorb pattern, or into the monorepo).

### Aggregate (provisional)

| Metric | Value |
|---|---|
| **Mean (all 71)** | **~1.30** (estimated, with 4 N/A pillars at 3) |
| **Pass (>=2.00)** | **NO** |
| **Domains ≥2.00** | 1 / 9 |
| **P0 gaps** | 8 (low confidence) |

---

### 1. Architecture (AX) — L1–L12 (provisional)

| Pillar | Score | Evidence (cached) |
|--------|-------|-------------------|
| L1 Module structure | **2** | Tree shows `src/`, `tests/`, `config/`, `docs/` (cached). No `crates/` (Python, N/A). |
| L2 Bounded contexts | **1** | README description: "load_config, setup_logging, parse_args, iso_now, truncate" — 5 utils, modest scope. |
| L3 API design | **1** | No public-API doc visible. |
| L4 Hexagonal ports-and-adapters | **0** | No `adapters/`, `domain/`, `ports/` layout. |
| L5 Dependency direction | **2** | `pyrightconfig.json` present → type-checking enforced. |
| L6 Cloud readiness | **1** | Python lib; no Dockerfile. |
| L7 Threat modeling | **0** | No security doc visible. |
| L8 Inter-service contracts | **1** | Utility lib — N/A but conservative. |
| L9 Backward compatibility | **1** | No semver policy visible. |
| L10 Loose coupling | **2** | 5 utility functions are independent. |
| L11 Portability | **3** | Pure Python; MIT license (cached). |
| L12 Service mesh / gateway | **0** | N/A. |
| **Domain mean** | **1.17** | |

### 2. Performance — L13–L19

| Pillar | Score | Evidence |
|--------|-------|----------|
| L13–L19 | **1.0 each** | No benchmarks, no SLOs, no perf story for a 5-function utility lib. |
| **Domain mean** | **1.00** | |

### 3. Quality / Correctness — L20–L27

| Pillar | Score | Evidence |
|--------|-------|----------|
| L20 Acceptance criteria | **1** | README describes purpose. |
| L21 Code review rigor | **Unknown** | PRs endpoint returns 404 (deleted repo). |
| L22 Static analysis | **2** | `pyrightconfig.json` (cached). |
| L23 Coding standards | **1** | `.editorconfig` + `.gitattributes` present (cached). |
| L24 Error handling | **1** | No exception taxonomy visible. |
| L25 Supply-chain integrity | **1** | `CITATION.cff` + `FUNDING.yml` exist but no SBOM/deny. |
| L26 Reliability / fault tolerance | **1** | Utility lib — N/A but conservative 1. |
| L27 Test architecture | **2** | `tests/` directory present (cached); content unknown. |
| **Domain mean** | **1.25** | |

### 4. Developer Experience (DX) — L28–L37

| Pillar | Score | Evidence |
|--------|-------|----------|
| L28 Build system | **2** | `pyproject.toml` present (cached). |
| L29 CI/CD pipeline | **0** | No `.github/workflows/` in cached tree. |
| L30 Dev environment setup | **1** | No `mise.toml`/`.python-version`/`.devcontainer`. |
| L31 Local development loop | **2** | `CONTRIBUTING.md` present (cached). |
| L32 Debug ergonomics | **1** | Standard Python tooling only. |
| L33 Hot reload | **0** | N/A (lib). |
| L34 Onboarding time-to-first-PR | **2** | `AGENTS.md` (875 B — substantial), `CLAUDE.md`. |
| L35 Release process | **Unknown** | Tags endpoint returns 404. |
| L36 Contribution friction | **1** | No PR template confirmed (`.github/` not in cached tree). |
| L37 Dev container / nix flake | **0** | Neither present. |
| **Domain mean** | **1.00** | |

### 5. User Experience (UX) — L38–L45

| Pillar | Score | Evidence |
|--------|-------|----------|
| L38 Learnability | **2** | `README.md` 6225 B (cached). |
| L39 Operability | **1** | Utility lib — minimal ops surface. |
| L40 i18n / localization | **3 (N/A)** | No UI. |
| L41 a11y / WCAG | **3 (N/A)** | No UI. |
| L42 Error messages | **1** | Not documented. |
| L43 User feedback loops | **1** | No issue template confirmed. |
| L44 Documentation discoverability | **2** | `docs/` present (cached). |
| L45 First-run experience | **2** | README quickstart implied. |
| **Domain mean** | **1.78** | (N/A L40/L41) |

### 6. Security — L46–L55

| Pillar | Score | Evidence |
|--------|-------|----------|
| L46 IAM | **1** | N/A. |
| L47 Data protection | **1** | No data-at-rest. |
| L48 Threat-aware API design | **0** | No threat model. |
| L49 Authentication | **1** | N/A. |
| L50 Cryptography | **0** | No crypto deps confirmed. |
| L51 Audit log integrity | **0** | No audit logging. |
| L52 Multi-tenant isolation | **0** | N/A. |
| L53 Input validation | **1** | No `pydantic`/`beartype`/validator deps confirmed. |
| L54 Build/deploy hardening | **1** | No SLSA/cosign visible. |
| L55 Vulnerability management | **0** | No `dependabot.yml`/pip-audit. |
| **Domain mean** | **0.50** | |

### 7. Observability & Ops — L56–L63

| Pillar | Score | Evidence |
|--------|-------|----------|
| L56 Structured logging | **1** | `setup_logging` is a documented function (README). |
| L57 Metrics (RED/USE) | **0** | No metrics. |
| L58 Distributed tracing | **0** | No OTEL. |
| L59 Alerting / SLO monitoring | **0** | N/A. |
| L60 Deployment automation | **0** | No release workflow. |
| L61 Incident response | **0** | SECURITY.md MISSING (404 at second probe). |
| L62 Backup / restore | **0** | N/A. |
| L63 Capacity planning | **0** | N/A. |
| **Domain mean** | **0.12** | |

### 8. Documentation & SSOT — L64–L68

| Pillar | Score | Evidence |
|--------|-------|----------|
| L64 README quality | **3** | 6225 B README (cached); description: "Shared Python utility library for the Phenotype org: load_config, setup_logging, parse_args, iso_now, truncate". |
| L65 ADR tracking | **0** | No `ADR.md`/`docs/adr/`. |
| L66 SSOT conventions | **1** | `docs/` exists (cached). |
| L67 API reference docs | **1** | No `docs/` hosting visible. |
| L68 Code-level documentation | **1** | Not enforced. |
| **Domain mean** | **1.20** | |

### 9. Governance & Sustainability — L69–L71

| Pillar | Score | Evidence |
|--------|-------|----------|
| L69 OpenSSF Best Practices | **0** | No scorecard workflow. |
| L70 Roles & responsibilities | **1** | CODEOWNERS present in cached tree but second probe returns 404. **State ambiguous.** |
| L71 Sustainability | **0** | **Repo deleted** between two probes 30 seconds apart. |
| **Domain mean** | **0.33** | |

---

## Delta Summary

**CRITICAL FINDING**: This repo no longer exists on KooshaPari as of 2026-06-20. The cached first-probe data is the only evidence available. Recommended actions:

1. **Immediate**: Determine whether the repo was deleted, transferred, or renamed. Search the fleet for `phenotype-py-utils` replacements.
2. **Recovery**: If a replacement exists (e.g., monorepo path or new canonical name), re-run the 71-pillar scoring against it.
3. **Migration log**: This finding file becomes part of the audit trail showing the deletion event.

**P0 items for the replacement repo:**
- All 8 P0 domains (Architecture, Performance, Quality, DX, Security, Observability, Docs, Governance) require attention.
- If the work is re-homed in the monorepo under `phenotype-py-utils/`, the substrate-quality bar (ADR-023 Rule 3.1: 80% coverage, OTLP, CI gate, spec) must be applied.

**Evidence links (probe commands run 2026-06-20 via `gh api`):**
- `gh repo view KooshaPari/phenotype-py-utils --json ...` → returned metadata on first probe, HTTP 404 on second.
- `gh api graphql -f query='{ repository(...) }'` → `repository: null`, `NOT_FOUND`.
- `gh api graphql -f query='{ search(...) }'` → empty result.
- `gh api repos/KooshaPari/phenotype-py-utils/{branches,tags,contents,commits}` → 404 on second probe.

---

*Generated for ADR-041 weekly refresh (T2A batch probe, 2026-06-20). Repo deleted/transfered during probe window — scores are provisional based on cached first-probe snapshot only.*