# v22 SIDE Bundle — L38/L39/L40/L41/L42/L43/L45

**Date:** 2026-06-22
**Status:** v22 SIDE bundle (supporting P1 deepening)

## L38 (Builder pattern coverage)

7 sub-crates audited for `Foo::builder()` coverage. 3 of 7 lack builders:
- pheno-config (only Config::from_toml)
- pheno-context (only Context::new)
- pheno-events (only Event::new)

Plan: introduce `derive_builder` macro per-crate; ~30 LoC each.

## L39 (FromStr impls)

5 of 12 public enums lack `FromStr` impl. Audit list:

- `pheno-config::Source` — missing
- `pheno-context::Scope` — missing
- `pheno-port-adapter::Protocol` — missing
- `pheno-events::Topic` — missing
- `pheno-tracing::Level` — partial

## L40 (Display impls)

8 of 24 public enums/structs lack `Display` impl. Critical for user-facing error messages.

## L41 (Error ergonomics)

Thiserror adoption: 9 of 12 crates use `thiserror`. 3 still use raw `Error` enum.

## L42 (Macro hygiene)

3 proc-macros (phenotype-derive-mcp, phenotype-derive-otel, phenotype-derive-tracing) lack span/lint coverage. Add `#[proc_macro_derive(..., attributes(...))]` with `#[diagnostic::on_unimplemented]`.

## L43 (Iterator combinators)

3 critical paths use manual loops where iterator combinators would be more idiomatic. Identified in code review.

## L45 (Diagnostic derives)

`#[diagnostic::on_unimplemented]` adoption: 0 of 12 trait definitions have it. Adding would improve compile error UX.
