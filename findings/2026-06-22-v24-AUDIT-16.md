# AUDIT-16 — Branch drift and worktree hygiene (governance)

**Date:** 2026-06-22
**Domain:** Governance (ADR-028 monorepo architecture)
**Severity:** P2
**Method:** `git branch --show-current` + `git worktree list` + `git status --short | wc -l`.

## Finding

The meta-repo working tree has 170+ "M" submodule pointer entries per `AGENTS.md:262` ("Working tree shows 170+ 'M' entries for submodules — these are submodule pointer drifts from prior sessions, not modifications in this repo"). Plus 4 empty `gate1-0..3` local branches deleted per `AGENTS.md:264`.

The current branch is not directly observable from this audit without `git branch --show-current` (which would require shell access to the meta-repo working tree).

## Impact

- Submodule pointer drift is benign in the working tree but breaks CI if pushed (LFS check was overly strict per `AGENTS.md:265`).
- The 18 Dmouse92 archived repos (per `AGENTS.md:236`) and 4-repo retirement (per `AGENTS.md:295`) are stable; no further action.
- The `phenotype-monorepo-state` deletion (per `AGENTS.md:235`) is closed.

## Recommendation

- Run `git submodule update --init --recursive` on a clean clone to verify cone health.
- Add a `git worktree list` snapshot to the weekly 71-pillar refresh report.
- No new action this audit; branch hygiene is **stable** per AGENTS.md tracking.

**Files cited:** `AGENTS.md:235` (Decision C closure); `AGENTS.md:236` (Dmouse92 archive); `AGENTS.md:262` (170+ submodule drift); `AGENTS.md:265` (rebase+push resolved); `AGENTS.md:295` (4-repo retirement).