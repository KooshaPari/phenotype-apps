# 71-Pillar Refresh: `KooshaPari/Settly`

**Date**: 2026-06-20
**Cycle**: ADR-041 weekly refresh (Monday 09:00 PDT)
**Schema**: [findings/71-pillar-2026-06-17-schema.md](findings/71-pillar-2026-06-17-schema.md)
**Scorer**: T2A batch probe (read-only `gh api`)
**Repo state at probe time**:
- `nameWithOwner`: KooshaPari/Settly
- `isPrivate`: false · **`isArchived`: true** · `isEmpty`: false
- `primaryLanguage`: Rust · `diskUsage`: 280 KB
- `pushedAt`: 2026-06-19T02:56:26Z · `createdAt`: 2026-03-25T16:00:02Z
- `defaultBranch`: `main`
- `licenseInfo`: MIT
- `branches`: 19 · `tags`: 0 · `open_prs`: 10 · `closed_prs`: 23 · `releases`: 0 · `issues_open`: 14
- `languages`: Rust 54,944, Shell 10,676
- Last commit: 2026-05-29T03:58:47Z
- **NOTE: Settly is now also a sub-crate of `Configra` (`crates/settly`) — this top-level repo is ARCHIVED.** ADR-031 documents the Configra absorb.

## Aggregate

| Metric | Value |
|---|---|
| **Mean (all 71)** | **1.97** |
| **Pass (>=2.00)** | **NO** (just below threshold) |
| **Domains ≥2.00** | 4 / 9 |
| **P0 gaps (mean<2.00)** | 5 |

**Verdict**: Strong DX & Architecture; weak Operations, Security, Performance. Settly is the *most complete* of the 5 T2A repos in tooling coverage (15 workflows, dependabot, all templates), but is **archived** — the canonical home is now `Configra/crates/settly`. Score reflects archived status (no ongoing activity).

---

## 1. Architecture (AX) — L1–L12

| Pillar | Score | Evidence |
|--------|-------|----------|
| L1 Module structure | **3** | `src/` has `adapters/`, `application/`, `domain/`, `infrastructure/`, `lib.rs` — hexagonal layout (Settly/src tree). |
| L2 Bounded contexts | **3** | Clear DDD layering: `domain/config`, `domain/layers`, `domain/sources`, `domain/validation`, `domain/ports`, `domain/errors` (README §Architecture). |
| L3 API design | **2** | README documents "Layered Configuration", "Validation", "Multiple Formats" — pub API not visible at tree level. |
| L4 Hexagonal ports-and-adapters | **3** | `domain/ports/` directory explicit; `adapters/sources/` matches. |
| L5 Dependency direction | **2** | `Cargo.toml` deps: `tokio`, `serde`, `validator`, `sqlx`, `redis` — all flow inward to domain. |
| L6 Cloud readiness | **1** | `sqlx` + `redis` suggest cloud-ready persistence; no Dockerfile/cloud config visible. |
| L7 Threat modeling | **0** | No `threat-model.md`, no `docs/security/`. |
| L8 Inter-service contracts | **2** | `ports/` interface definitions; explicit contracts per hexagonal pattern. |
| L9 Backward compatibility | **1** | `Cargo.toml` version `0.1.0`, no semver policy doc. |
| L10 Loose coupling | **3** | Port/adapter split enforces decoupling. |
| L11 Portability | **2** | MIT OR Apache-2.0; rust-version 1.75; PostgreSQL/Redis are external but abstracted via sqlx/redis crates. |
| L12 Service mesh / gateway | **0** | Library; no service surface. Conservative 0. |
| **Domain mean** | **1.83** | |

## 2. Performance — L13–L19

| Pillar | Score | Evidence |
|--------|-------|----------|
| L13 Latency budgets | **1** | `benches/perf.rs` exists (1 file). No published SLOs. |
| L14 Resource efficiency | **2** | Pure-Rust; sqlx + redis are connection-pooled by default. |
| L15 Throughput | **1** | `benches/perf.rs` (single file); no published results. |
| L16 Scalability | **2** | sqlx + Redis are horizontally scalable. |
| L17 Cold start | **2** | Pure-Rust init; no heavy runtime. |
| L18 Concurrency model | **2** | `tokio` async; sqlx is async-native. |
| L19 Cost awareness | **1** | No resource budgets documented. |
| **Domain mean** | **1.57** | |

## 3. Quality / Correctness — L20–L27

| Pillar | Score | Evidence |
|--------|-------|----------|
| L20 Acceptance criteria | **2** | `TEST_COVERAGE_MATRIX.md` (visible in tree), `VERIFICATION_POLICY.md`, `QA_MATRIX.md` exist. |
| L21 Code review rigor | **2** | 23 closed PRs + CODEOWNERS + PULL_REQUEST_TEMPLATE — review is structured. |
| L22 Static analysis | **3** | `deny.toml` (1135 B), `clippy.toml`, `cargo-deny.yml` workflow, `sast.yml` + `security-deep-scan.yml` + `trufflehog.yml` workflows. |
| L23 Coding standards | **3** | `clippy.toml`, `_typos.toml`, `STANDARDS.md` (in tree). |
| L24 Error handling | **2** | `thiserror` + `anyhow` (Cargo.toml). |
| L25 Supply-chain integrity | **2** | `cargo-deny.yml` workflow, `dependabot.yml` (273 B), SBOM not visible. |
| L26 Reliability / fault tolerance | **1** | sqlx + Redis provide some resilience; no chaos tests. |
| L27 Test architecture | **2** | `fuzz/` directory exists (in tree); no `tests/` directory at root visible. |
| **Domain mean** | **2.12** | |

## 4. Developer Experience (DX) — L28–L37

| Pillar | Score | Evidence |
|--------|-------|----------|
| L28 Build system | **3** | Cargo workspace, `Taskfile.yml` (8187 B), `rust-toolchain.toml`, `nextest.toml`, `mise.toml`. |
| L29 CI/CD pipeline | **3** | **15 workflows**: `ci.yml`, `coverage.yml`, `quality-gate.yml`, `release.yml`, `release-drafter.yml`, `benchmarks.yml`, `pages-deploy.yml`, `pre-commit.yml`, `auto-merge.yml`, `legacy-tooling-gate.yml`, `sast.yml`, `security-deep-scan.yml`, `security-guard.yml`, `security.yml`, `trufflehog.yml`. |
| L30 Dev environment setup | **3** | `Taskfile.yml`, `mise.toml`, `.env.example`, `rust-toolchain.toml`, `.config/`, `.githooks/`. |
| L31 Local development loop | **3** | `CONTRIBUTING.md` (184 B), `.githooks/`, `Taskfile.yml` quickstart. |
| L32 Debug ergonomics | **2** | `.githooks/`, `pre-commit.yml` workflow. |
| L33 Hot reload | **3** | README: "Hot Reload: Watch and reload configuration files". |
| L34 Onboarding time-to-first-PR | **3** | `AGENTS.md` (568 B), `CLAUDE.md` (548 B), `CONTRIBUTING.md`, `FUNCTIONAL_REQUIREMENTS.md`. |
| L35 Release process | **3** | `release.yml` (1294 B), `release-drafter.yml` (425 B), `pages-deploy.yml`, but **0 GitHub Releases / 0 tags** (repo is archived pre-tag). |
| L36 Contribution friction | **3** | `PULL_REQUEST_TEMPLATE.md`, `ISSUE_TEMPLATE/bug.yml`, `ISSUE_TEMPLATE/feature.yml`. |
| L37 Dev container / nix flake | **1** | No `.devcontainer/`, no `flake.nix` visible. |
| **Domain mean** | **2.70** | |

## 5. User Experience (UX) — L38–L45

| Pillar | Score | Evidence |
|--------|-------|----------|
| L38 Learnability | **3** | README has full architecture diagram, formats list, ASCII tree of modules. |
| L39 Operability | **2** | `TASKFILE.yml` + `mise.toml` define operational tasks. |
| L40 i18n / localization | **3 (N/A)** | Library, no UI. **N/A eligible.** |
| L41 a11y / WCAG | **3 (N/A)** | No UI. **N/A eligible.** |
| L42 Error messages | **2** | `thiserror` enums; no error catalog visible. |
| L43 User feedback loops | **2** | `ISSUE_TEMPLATE/bug.yml` + `feature.yml`. |
| L44 Documentation discoverability | **3** | `docs/journeys/`, `docs/research/`, `docs/stories/`, `docs/traceability/`, `SPEC.md` (583 B), `FUNCTIONAL_REQUIREMENTS.md`. |
| L45 First-run experience | **3** | README quickstart + `examples/` would be ideal (no `examples/` dir visible, so deducted). Score 2. |
| **Domain mean** | **2.56** | (L40/L41 N/A=3) |

## 6. Security — L46–L55

| Pillar | Score | Evidence |
|--------|-------|----------|
| L46 IAM | **1** | N/A (library); conservative 1. |
| L47 Data protection | **2** | `validator` crate enforces input schemas; secrets handling not explicit. |
| L48 Threat-aware API design | **1** | No threat model doc. |
| L49 Authentication | **1** | N/A (library). |
| L50 Cryptography | **1** | No crypto deps in `Cargo.toml`. |
| L51 Audit log integrity | **2** | SQL persistence via `sqlx` enables audit logs (per README "full audit trails" claim). |
| L52 Multi-tenant isolation | **2** | PostgreSQL schema-level isolation achievable. |
| L53 Input validation | **3** | `validator = "0.18"` with `derive` feature (Cargo.toml). |
| L54 Build/deploy hardening | **2** | `deny.toml` (1135 B), `security.yml`, `security-deep-scan.yml`, `security-guard.yml`. |
| L55 Vulnerability management | **3** | `.github/dependabot.yml` (273 B), `cargo-deny.yml` workflow, `trufflehog.yml`. |
| **Domain mean** | **1.80** | |

## 7. Observability & Ops — L56–L63

| Pillar | Score | Evidence |
|--------|-------|----------|
| L56 Structured logging | **2** | `tracing` not visible in Cargo.toml deps; logs via sqlx middleware available. |
| L57 Metrics (RED/USE) | **0** | No metrics crate in deps. |
| L58 Distributed tracing | **1** | No OTLP/tracing-opentelemetry in deps. |
| L59 Alerting / SLO monitoring | **0** | No SLOs published. |
| L60 Deployment automation | **1** | `release.yml` + `pages-deploy.yml` workflows exist but repo is archived. |
| L61 Incident response | **1** | `SECURITY.md` (239 B — minimal: "email maintainers, allow time"). |
| L62 Backup / restore | **1** | SQL persistence; backup strategy not documented. |
| L63 Capacity planning | **1** | No capacity model. |
| **Domain mean** | **0.88** | |

## 8. Documentation & SSOT — L64–L68

| Pillar | Score | Evidence |
|--------|-------|----------|
| L64 README quality | **3** | 2520 B; architecture diagram, feature list, status banner. |
| L65 ADR tracking | **2** | `ADR.md` (77 B — minimal stub); `CLAUDE.md` (548 B). |
| L66 SSOT conventions | **3** | `SSOT.md` (in tree), `PLAN.md`, `PRD.md`, `STANDARDS.md`, `VERIFICATION_POLICY.md`. |
| L67 API reference docs | **2** | README links `docs.rs/settly` (in Cargo.toml `[package].documentation`). |
| L68 Code-level documentation | **2** | No rustdoc enforcement visible. |
| **Domain mean** | **2.40** | |

## 9. Governance & Sustainability — L69–L71

| Pillar | Score | Evidence |
|--------|-------|----------|
| L69 OpenSSF Best Practices | **2** | `trufflehog.yml`, `security-deep-scan.yml`, `sast.yml` workflows; no OpenSSF scorecard workflow. |
| L70 Roles & responsibilities | **2** | `CODEOWNERS`: `* @kooshapari` (single owner, scoped explicitly). |
| L71 Sustainability | **0** | **Repo is ARCHIVED (read-only marker)**. Per ADR-031 the canonical home is `Configra/crates/settly`. Future sustainability = 0 at this top-level repo. |
| **Domain mean** | **1.33** | |

---

## Delta Summary

**Notable changes vs prior cycle:**
- 2026-06-19 archived state: per ADR-031 (Configra absorb) this repo entered read-only. PushedAt (2026-06-19) was the last admin action.
- 10 open PRs remain unfunded (per `gh api /pulls?state=open`); they will not be merged here.

**P0 items for remediation (domain mean < 2.00):**
- **Observability & Ops (0.88)**: Add metrics + tracing-opentelemetry deps; publish SLOs; expand SECURITY.md.
- **Governance & Sustainability (1.33)**: Repo is archived by design — no remediation possible. L70/L71 transfer to Configra.
- **Architecture (1.83)**: Add threat-model.md (L7), cloud-readiness manifest (L6).
- **Security (1.80)**: Add cryptographic primitives if secrets handling is intended (L50).
- **Performance (1.57)**: Publish benchmarks from `benches/perf.rs`.

**Evidence links (probe commands run 2026-06-20 via `gh api`):**
- `gh repo view KooshaPari/Settly --json ...` → confirmed archived state.
- `gh api repos/KooshaPari/Settly/git/trees/main` → 40+ root entries including hexagonal `src/` layout.
- `gh api repos/KooshaPari/Settly/contents/.github/workflows` → 15 workflow YAMLs.
- `gh api repos/KooshaPari/Settly/contents/Cargo.toml` → confirmed `validator`, `sqlx`, `redis` deps.

---

*Generated for ADR-041 weekly refresh (T2A batch probe, 2026-06-20). Read-only `gh api` only — no pushes performed.*