# AUDIT-06 — Connection-pool implementation inventory (L18)

**Date:** 2026-06-22
**Pillar:** L18 (Connection pool tuning, paired with L17)
**Severity:** P1
**Method:** Same grep as AUDIT-05; + `find . -name "*.toml" | xargs grep -l "bb8\|deadpool\|sqlx"`.

## Finding

Connection-pool-shaped code in the cone:

- `helios-cli/harness/src/harness/http_pool.py` (Python HTTP pool, single source)
- `OmniRoute-b10/go.mod:11` declares `github.com/jackc/pgx/v5 v5.9.2` (PostgreSQL pool via pgx)
- `melosviz/backend/.venv/lib/python3.14/site-packages/httpcore/_async/http_proxy.py` (vendored httpcore, not fleet-owned)

There is **no fleet-owned connection-pool substrate crate** (e.g. `pheno-connection-pool`). Pool config is scattered per repo. No `cargo` workspace member is named `pheno-pool` or `pheno-conn`.

## Impact

L18 acceptance criterion (`pool exhaustion 0/1000 trials` per `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:46`) cannot be validated fleet-wide without an inventory of pool types per repo. The T3 bench harness needs to enumerate per-crate pool types before it can run.

## Recommendation

- Add a per-repo inventory step to T3: list pool type + max-size knob for each Cargo.toml that declares `bb8`, `deadpool`, `sqlx`, or `pgx`.
- Add pool tuning knobs to the config knob doc alongside cache knobs (single doc, two sections).
- Consider ADR candidate for "pheno-connection-pool as canonical substrate" in v25+ (cycle 15+) — out of scope for cycle 14.

**Files cited:** `helios-cli/harness/src/harness/http_pool.py`; `OmniRoute-b10/go.mod:11`; `plans/2026-06-22-v24-71-pillar-cycle-14-p1.md:46`.