# v14 Plan — 71-Pillar Cycle 3 P0 Remediation

**Branch:** `chore/v14-71-pillar-cycle-3-p0-2026-06-20`
**Source cycle-3 probe:** `findings/2026-06-20-71-pillar-cycle-3-probe.md`
**Target:** close 8 more P0 pillars (26 → 18), fleet mean 2.6 → 2.7

## Tracks

| # | Pillar | Track | Files | Wave |
|---|---|---|---|---|
| **T1** | L3 (deps graph) | Add `cargo-deps` SVG to 11 fleet crates | `scripts/cargo-deps-graph.sh` (40 lines) + CI step in each crate | A |
| **T2** | L4 (module deps) | `cargo-modules --deny cyclic` in 5 workspaces | `.cargo/config.toml` per crate + CI step | A |
| **T3** | L18 (perf budget) | Document per-route p99 budgets in 3 crates | `docs/perf-budget.md` × 3 + criterion test updates | A |
| **T4** | L22 (mut testing) | `cargo-mutants` for pheno-port-adapter + pheno-errors | `.cargo-mutants.toml` + GitHub Action | A |
| **T5** | L25 (concurrency) | `loom` for pheno-otel + pheno-port-adapter | `tests/loom_*.rs` per crate | A |
| **T6** | L34 (dev-loop time) | `cargo nextest` + 3-min CI target | `.config/nextest.toml` + ci.yml update | A |
| **T7** | L62 (error rate) | OTel `errors.count` metric in all crates | `pheno-otel/src/metrics.rs` (NEW) + adoption | A |
| **T8** | L68 (rustdoc) | `#![deny(missing_docs)]` in 8 lib crates | 1-line edit × 8 | A |

## Wave Schedule

**Wave A** (all 8 tracks, fully parallel): 1.5h wall via parallel subagent dispatch.
**Wave B** (none — no inter-deps).
**Wave C** (sequential): cycle 4 probe + v15 plan. 1h.

## Targets Per Track

| Track | Repo(s) | Lines | Score lift | Files |
|---|---|---|---|---|
| T1 | 11 crates | ~20 each | 1→3 | 1 script + 11 CI steps |
| T2 | 5 workspaces | ~10 each | 1→3 | 5 config + 5 CI steps |
| T3 | pheno-port-adapter, pheno-mcp-router, pheno-otel | ~30 each | 2→3 | 3 docs + criterion updates |
| T4 | pheno-port-adapter, pheno-errors | ~50 + ~30 mutants | 1→3 | 2 config + 2 actions |
| T5 | pheno-otel, pheno-port-adapter | ~80 | 1→3 | 4 loom tests |
| T6 | 5 critical crates | ~15 each | 2→3 | 5 nextest configs + 5 ci.yml |
| T7 | 8 crates | ~120 (otel crate) + ~5 (adopters) | 2→3 | 1 lib + 8 adopters |
| T8 | 8 lib crates | 1 each | 1→3 | 8 lib.rs edits |

## Critical-Path Sub-Tasks

1. **T7 is the load-bearing track** — new `pheno-otel/src/metrics.rs` (120 lines) requires careful API design (counter vs histogram, label cardinality, OTLP wire format). Allocate 1.5h.
2. **T4 requires nightly toolchain** — `cargo-mutants` depends on Rust nightly. Verify with `rustup toolchain list` before starting.
3. **T5 requires `loom` as dev-dep** — verify `loom` is on crates.io and compatible with current Rust 1.83.
4. **T8 is fastest** — 8 single-line edits, parallel-safe.

## Pre-Flight Gate

- [ ] Confirm `cargo-deps`, `cargo-modules`, `cargo-mutants`, `cargo-nextest`, `loom` installable
- [ ] Confirm `pheno-otel` has `metrics` API surface ready (or extend)
- [ ] Confirm 8 lib crates compile with `#![deny(missing_docs)]`
- [ ] Set up `chore/v14-71-pillar-cycle-3-p0-2026-06-20` branch

## Out-of-Scope (Deferred to v15+)

- L6 (no cycles in dep graph) — requires fleet-wide circular-dep audit
- L11.3 (24h soak test) — needs heavy-runner; defer to heavy-runner wave
- L48 (SBOM diff) — v13 T3 covered initial SBOM; v15 will add diff policy
- L49 (vuln response runbook) — needs incident-response framework first

## Coordination

Per ADR-041 (refresh cadence weekly Mon 09:00 PDT), this plan activates immediately.
Per ADR-049 (drift detector), fleet's auto-rebase handles merge conflicts.

Refs: ADR-024, ADR-041, ADR-048, ADR-049, cycle-3 probe