# v18 Plan — 71-Pillar Cycle 8 P0 + Security P1

**Date:** 2026-06-21
**Branch:** `chore/v18-71-pillar-cycle-8-p0-2026-06-21`
**Supersedes:** v17 (cycle 7)
**Cadence:** per ADR-041 (weekly Mon 09:00 PDT)
**Cumulative at start:** 44 of 47 P0 closed (94%); 9-pillar mean 3.00; 11.5-pillar mean 2.95

## Target

Close the 3 remaining P0 pillars (L17 compliance, L18 data classification, L51 SOC2 evidence) + 7 Security P1 pillars (domain mean 2.40 → 3.00). Target 47/47 P0 = 100% closure; 11.5-pillar mean 2.95 → 3.00; Security domain 2.40 → 3.00.

## Worktree pattern (per ADR-018 PRCP + ADR-023 Rule 1)

All substrate-level work (Cargo.toml changes, `tests/chaos_*.rs`, `i18n/*.ftl`, `.clippy.toml`, `#![deny(...)]`) happens in **per-crate worktrees** at `/private/tmp/v18-<track>-<crate>-<date>`. This monorepo (the `repos/` coordination hub) holds the **plans + findings + governance + rollup**, not the substrate source. The plan in this document is the **coordination contract**; the actual files land in the per-crate worktrees.

## Wave A (2 tracks, parallel)

### T1 — L17 Compliance / Audit (FedRAMP/IL5 baseline)

Deliverable: `docs/architecture/compliance-baseline.md` (300 lines) listing:
- FedRAMP Rev. 5 control families mapped to 71-pillar pillars (AC, AU, CM, IA, SI, SC, RA)
- IL5 boundary (CUI//SP-PRIV//SP-EXPT) — 9 additional controls
- Per-crate compliance posture for the 5 substrate repos (pheno-config, pheno-flags, pheno-errors, pheno-port-adapter, pheno-mcp-router)
- Audit-logging requirements (`#[tracing::instrument(level = "info", skip_all)]` on every public method that mutates state, OTLP sink to `pheno-tracing`)

### T2 — L18 Data Classification (DLP integration)

Deliverable: `docs/architecture/data-classification.md` (250 lines) + `pheno-classify` trait (new) providing:
- 4-tag enum: `Public | Internal | Confidential | Restricted`
- `#[classify(...)]` attribute macro for tagging fields
- Lint via `scripts/check-classify.sh` — verifies every `pub struct` with `String`/`Vec<u8>` field has a `classify` attribute
- Adopted in 5 substrate repos

## Wave B (1 track)

### T3 — L51 SOC2 Evidence (automated control tests)

Deliverable: `.soc2/` directory at monorepo root with:
- `controls/CC1.1..CC9.2.toml` (33 Common Criteria controls) — each: assertion, evidence source, test path
- `tests/soc2_controls.rs` — 33 integration tests, one per control, each fetching the named evidence and asserting presence
- `.github/workflows/soc2-evidence.yml` — weekly cron + push-to-main, uploads evidence bundle as workflow artifact (90-day retention)
- `docs/soc2-evidence-runbook.md` — how to add a new control, how to query historical evidence

## Wave C (7 tracks — Security P1 deepening)

| Track | Pillar | Deliverable |
|-------|--------|-------------|
| T4 | L46 (secrets management) | `pheno-secret-scan` enforcement in CI — blocks any commit with a 1.0+ entropy string. Adopted in 5 substrate repos. |
| T5 | L47 (input validation) | `#![deny(missing_docs)]` + `clippy.toml` rule for `unwrap_used`/`expect_used` outside `tests/`. 5 repos. |
| T6 | L48 (SBOM) | CycloneDX 1.5 SBOM emitted per release. Adopted in 5 repos. |
| T7 | L49 (dep vuln) | `cargo audit --deny warnings` + `pip-audit --strict` in CI. Adopted in 5 repos. |
| T8 | L50 (sigstore) | cosign signing of release artifacts. Adopted in 5 repos. |
| T9 | L52 (mTLS) | mTLS for cross-crate HTTP boundaries (phenotype-router ↔ substrate). New: `pheno-mtls` crate. |
| T10 | L53 (authn) | OIDC client for fleet-crossing calls. New: `pheno-oidc` crate. |

## Wave D (1 track)

### T11 — Security P1 rollup

Deliverable: `findings/2026-06-21-v18-security-rollup.md` showing:
- 7 Security pillars moved from 2.40 → 3.00
- Per-pillar evidence (test counts, audit pass rates, SBOM/cosign artifacts)
- Cross-walk with the FedRAMP/IL5 baseline from T1

## Wave E (closure)

- Cycle 8 probe: `findings/2026-06-21-v18-cycle-8-probe.md`
- v19 plan: `plans/2026-06-21-v19-71-pillar-cycle-9-p0.md`
- AGENTS.md refresh
- Final state: 47/47 P0 closed (100%); Security domain 2.40 → 3.00; 11.5-pillar mean = 3.00

## Acceptance criteria

- [ ] All 11 tracks shipped
- [ ] 47 of 47 P0 pillars closed (100%)
- [ ] Security domain mean = 3.00
- [ ] 11.5-pillar mean = 3.00
- [ ] 0 net content loss
- [ ] No new arch debt
- [ ] All 7 Security P1 pillars adopted in ≥ 5 substrate repos

## ADR anchors

- ADR-018 (PRCP pattern) — worktree parallelism
- ADR-023 (Agent-effort governance) — device-fit gate for v18 work
- ADR-040 (Test coverage gates per tier) — 80% lib / 70% framework / 60% service
- ADR-041 (71-pillar refresh cadence) — weekly Mon 09:00 PDT
- ADR-042B (Substrate quality bar) — codifies adoption bar for L46-L53

## Estimated wall time

- Wave A (T1+T2): 4 hours (compliance docs are dense)
- Wave B (T3): 3 hours (33 control files)
- Wave C (T4-T10): 14 hours (7 tracks, ~2h each)
- Wave D (T11): 1 hour (rollup)
- Wave E (closure): 1 hour
- **Total:** ~23 hours wall; recommend 3 sessions × 8h OR 5 sessions × 5h

## Device-fit (per ADR-023 Rule 1)

| Track | Heavy? | MacBook-OK? |
|-------|--------|-------------|
| T1 compliance docs | No (markdown) | YES |
| T2 data classification (trait) | No (Rust trait) | YES |
| T3 SOC2 evidence | No (TOML + integration tests) | YES |
| T4 pheno-secret-scan | No (shell script + CI) | YES |
| T5 input validation (clippy) | Borderline (clippy is offline) | YES |
| T6 SBOM | No (CI job) | YES |
| T7 dep vuln | No (CI job) | YES |
| T8 cosign | Borderline (cosign binary) | YES with --offline mode |
| T9 mTLS (new crate) | Yes (network in tests) | NO heavy-runner |
| T10 OIDC (new crate) | Yes (network in tests) | NO heavy-runner |
| T11 rollup | No (markdown rollup) | YES |

**Heavy work:** T9, T10 → `device: heavy-runner`. All other tracks: `device: macbook`.
