[package]
name = "tracely-core-fuzz"
version = "0.0.0"
edition = "2021"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
tracely = { path = ".." }

# Prevent this from interfering with workspaces
[workspace]
