# benchmarks/python — v12 T9 L57 perf regression (Python side)
# pytest-benchmark with regression group + warmup.

[pytest]
benchmark_groups = regression
benchmark_min_rounds = 5
benchmark_warmup = true
benchmark_warmup_iterations = 100
benchmark_disable_gc = true
testpaths = .
