# Specifications

- `build` must run the build commands for every detected language/component.
- `test` must run the test commands for every detected language/component.
- `lint` must run the relevant static checks for every detected language/component.
- `clean` must remove generated artifacts for the detected components.
