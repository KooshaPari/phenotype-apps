# Testing Strategy

- Validate the Taskfile itself with a syntax-oriented inspection and a repo status check.
- If the local toolchain is available, run the top-level `build`, `test`, and `lint` tasks after the edit.
- Keep the session notes concise and update them if the implementation changes.
