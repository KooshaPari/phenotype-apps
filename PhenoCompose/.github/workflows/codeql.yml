name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '27 4 * * 1'
  security-events: write
  actions: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        language: ["go", "javascript-typescript", "python", "actions"]
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v4@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      - uses: github/codeql-action/init@8c6e48dbe051ceb3015c19554831af1b43275f46 # v3
        with:
          languages: ${{ matrix.language }}
      - uses: github/codeql-action/analyze@8c6e48dbe051ceb3015c19554831af1b43275f46 # v3
        with:
          category: "/language:${{ matrix.language }}"

permissions:
  contents: read