# OmniRoute — 30-Pillar Audit (Reference)

> **Reference repo.** Scored 2026-06-16 against the 71-sub-pillar rubric in [`AUDIT-METHOD.md`](./AUDIT-METHOD.md). Evidence: file path + line range per cell.

## Pillar scores

| Pillar | Score | Evidence |
|--------|------:|----------|
| **A1** Hexagonal / port-adapter | 2 | `docs/architecture/ARCHITECTURE.md` (CLAUDE.md:30-43 layers), `open-sse/{handlers,executors,translator,transformer,services}` named as ports/adapters, but no strict `ports/` `adapters/` dir layout. |
| **A2** ADR coverage | 2 | `docs/adr/0001..0005` (5 ADRs, MADR-style), `CLAUDE.md` references them. No automated ADR-generation from PRs. |
| **A3** Dependency direction | 2 | CLAUDE.md:71-76 "Domain/Policy engine, cost rules, fallback logic", "Never write raw SQL in routes" — rules documented. No automated arch-unit test. |
| **A4** Module boundary discipline | 2 | `npm run check:cycles` exists; CLAUDE.md:26 mentions. No circular dep runtime gate. |
| **A5** Domain model richness | 2 | CLAUDE.md:42 "Domain/Policy: Policy engine, cost rules, fallback logic" + 3-layer resilience (CLAUDE.md:48-110) with typed circuit breaker states. |
| **X1** Lint baseline + ratchet | 3 | `quality-baseline.json` with ratchets (eslintWarnings 3669 down, eslintErrors 0 down eps 0, coverage 76.5% up, OpenAPI cov 38.3% up eps 0.5, i18n 80.1% up). `npm run quality:ratchet`. |
| **X2** Type strictness | 2 | `tsconfig.json: strict: false, strictNullChecks: true` — partial. CLAUDE.md:80 "strict: false, target ES2022". |
| **X3** Complexity budget | 1 | `quality-baseline.json` tracks aggregate metrics; no per-fn complexity gate. |
| **X4** Duplication budget | 1 | `.sonar*` not present; jscpd absent. |
| **X5** Dead code budget | 1 | `deadExports` field in `quality-baseline.json`; no enforcement gate visible. |
| **X6** Format enforcement | 3 | `.pre-commit-config.yaml` + `.husky/` (2 hooks); `lint-staged` per CLAUDE.md:130. |
| **D1** Spec traceability | 1 | `docs/operations/journey-traceability.md` exists; AGILE-style journey → spec map; no machine-checkable spec→code graph. |
| **D2** Journey maps | 2 | `docs/operations/journey-traceability.md` (only file in `docs/operations/`); CLAUDE.md references it. |
| **D3** Code comments ≠ "what" | 2 | CLAUDE.md hard-rule style: "Hard Rules" numbered 1-18, WHY-focused. Code review convention. |
| **D4** CHANGELOG discipline | 3 | `CHANGELOG.md` 951KB, 7796 lines, Keep-a-Changelog format; generated by `orhun/git-cliff-action` SHA-pinned in `release.yml`. |
| **D5** API reference | 3 | `docs/reference/openapi.yaml` 178KB, baseline-tracked (`openapiCoverage.pct 38.3% up eps 0.5`). |
| **D6** Architecture map | 3 | `docs/architecture/REPOSITORY_MAP.md` + `CODEBASE_DOCUMENTATION.md` + `ARCHITECTURE.md` + `RESILIENCE_GUIDE.md` + `QUALITY_GATES.md` + `AUTHZ_GUIDE.md`. CLAUDE.md "Reference Documentation" table with 25+ entries. |
| **U1** Design system adherence | 2 | `src/lib/sync/tokens.ts` design tokens, no per-component hex values. CLAUDE.md:21 "Rich UI mandate" referenced. |
| **U2** Component library wired | 1 | CLAUDE.md:21 "Radix, shadcn, Headless UI" mandated in CLAUDE.md, but no Radix/shadcn in `package.json`; custom Next.js UI. |
| **U3** Dark mode + light | 1 | CLAUDE.md "Dark mode first, light mode as secondary" mandated; no evidence of tokenized theme in code. |
| **U4** Typography discipline | 1 | CLAUDE.md:23 "monospace for technical/code UI elements, system sans for prose" mandated; no evidence in code. |
| **UX1** Empty states, loading, error | 1 | Convention documented in CLAUDE.md; not measured. |
| **UX2** Progressive disclosure | 1 | CLAUDE.md:22 mandated ("hover-to-expand, progressive disclosure, gallery views, tooltips"); not measured. |
| **UX3** Gallery / list / detail | 1 | Convention; not measured. |
| **AT1** WCAG 2.1 AA | 0 | No evidence of axe-core or contrast tooling. |
| **AT2** Keyboard nav | 0 | No evidence. |
| **AT3** Screen reader | 0 | No evidence. |
| **AT4** i18n coverage | 3 | `src/i18n/messages/` contains 50+ locales (ar, az, bg, bn, cs, da, de, en, es, fa, ...); `i18nUiCoverage.pct 80.1% up eps 0.5` in baseline. |
| **AT5** RTL support | 1 | `ar.json` and `fa.json` locales present; layout not confirmed RTL. |
| **T1** Unit coverage ≥60/60/60/60 | 3 | `quality-baseline.json`: coverage.statements 76.5, lines 76.5, functions 82, branches 73. All ≥60. `npm run test:coverage` (CLAUDE.md:14). |
| **T2** Integration tests | 3 | `tests/integration/`, `tests/live/`; CLAUDE.md:99 `npm run test:all`. `RUN_SERVICES_INT=1` gated per CLAUDE.md "Common Modification Scenarios". |
| **T3** E2E tests | 3 | `tests/e2e/` (3 spec files: combo-unification, proxy-registry, responsive); `npm run test:e2e` (CLAUDE.md:96). Playwright. |
| **T4** Contract tests | 2 | `tests/llm-security/`; `phenoContracts` mentions Prusti/Kani adapters. CLAUDE.md:97 "Protocol E2E (MCP+A2A)". |
| **T5** Bug-fix reproduction test | 3 | CLAUDE.md:103-120 "Bug fix / issue triage protocol (Hard Rule #18)": TDD preferred; examples given: #3090, #3113, #3052. |
| **T6** Both test runners green | 3 | CLAUDE.md:100-101 "Both test runners must pass: `npm run test:unit` (Node native) AND `npm run test:vitest` (MCP server, autoCombo, cache)"; both wired in CI. |
| **P1** Hot path benchmarks | 2 | `benches/2026-05-07-routing-benchmarks.json`; CLAUDE.md:13 `npm run test:vitest` includes benches. No continuous bench CI. |
| **P2** Profiling in CI | 0 | No evidence. |
| **P3** Bundle / binary size | 2 | `.size-limit.json` present. No CI size gate visible. |
| **P4** Cold start / p50 / p99 SLO | 1 | CLAUDE.md references RED metrics in OB2; no SLOs defined. |
| **P5** Cache hit rate | 1 | CLAUDE.md:42 "Cache check" in pipeline; no measurement. |
| **S1** SAST | 3 | `.github/workflows/audit.yml` (CodeQL) cron `0 9 * * 1` (weekly); `.zizmor.yml` present; SonarSource action SHA-pinned. |
| **S2** SCA / dep vulns | 1 | `.license-allowlist.json` (license policy); no explicit SCA in workflows. `dependency-review` not seen. |
| **S3** Secret scan | 2 | `.gitleaks.toml` present; pre-commit secret scan via `.pre-commit-config.yaml`. |
| **S4** Authn/authz | 3 | CLAUDE.md:88-95 + `docs/architecture/AUTHZ_GUIDE.md` + `src/server/authz/routeGuard.ts` (referenced in CLAUDE.md Hard Rule #15, #17). |
| **S5** Input validation | 3 | CLAUDE.md:88 "Validate all inputs with Zod schemas"; `src/shared/constants/providers.ts` Zod-validated at load. |
| **S6** Output sanitization | 3 | CLAUDE.md:90-92 "Error responses MUST route through `buildErrorBody()` or `sanitizeErrorMessage()`". `docs/security/ERROR_SANITIZATION.md`. |
| **S7** Threat model | 1 | `docs/security/` exists (10 docs); STRIDE not formalized. |
| **S8** SLSA Build L2 | 1 | `release.yml` exists; CLAUDE.md mentions SLSA; **no `attest-build-provenance` step in workflows**. |
| **S9** Action SHA pinning | 3 | All `uses:` in `.github/workflows/*.yml` are 40-char SHAs (verified `actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683`, etc.). |
| **Q1** 35 CI quality gates | 3 | `docs/architecture/QUALITY_GATES.md`; 26 jobs in `ci.yml`; lint (18) + docs-sync-strict (12) + quality-gate + test-vitest. |
| **Q2** Ratchets vs baseline | 3 | `quality-baseline.json` ratchets in 8+ metrics; `npm run quality:ratchet -- --update` only when improves (CLAUDE.md:148). |
| **Q3** Allowlist hygiene | 2 | CLAUDE.md:151-155 "Allowlist policy (short form): Fix the cause; use the allowlist only for pre-existing violations you cannot fix in the same PR. Add a comment with justification + issue number. Stale allowlist entries will be caught by the stale-enforcement added in Fase 6A.3." |
| **Q4** Coverage gap reports | 2 | CLAUDE.md:122-126 "Copilot coverage policy: When a PR changes production code and coverage is below 60% ... do not just report — add or update tests, rerun the coverage gate". |
| **E1** Worktree discipline | 2 | CLAUDE.md:132-135 mandates worktrees; not enforced by tooling. |
| **E2** Branch hygiene | 2 | CLAUDE.md:133 "Branch prefixes: `feat/`, `fix/`, `refactor/`, `docs/`, `test/`, `chore/`"; no branch-protection. |
| **E3** Commit format | 2 | CLAUDE.md:134 "Conventional Commits: `feat(db): add circuit breaker`"; enforced by lint-staged. |
| **E4** Co-author trailer policy | 3 | CLAUDE.md Hard Rule #16: "Never include `Co-Authored-By` trailers that credit an AI assistant, LLM, or automation account". |
| **E5** No `--no-verify` / `--no-gpg-sign` | 3 | CLAUDE.md Hard Rule #10: "Never bypass Husky hooks (`--no-verify`, `--no-gpg-sign`) without explicit operator approval". |
| **G1** CODEOWNERS | 3 | `CODEOWNERS` 21 lines, per-area ownership (`/src/providers/*`, `/src/api/*`, `/src/combo/*`, `/src/resilience/*`, `/src/security/*`, `/open-sse/*`, `/electron/*`, `/.github/*`). |
| **G2** SECURITY.md | 1 | `SECURITY.md` exists at root. |
| **G3** CONTRIBUTING.md | 1 | `CONTRIBUTING.md` exists at root. |
| **G4** LICENSE | 1 | `LICENSE` exists; CLAUDE.md context. |
| **G5** CODE_OF_CONDUCT.md | 1 | `CODE_OF_CONDUCT.md` exists at root. |
| **G6** Audit log retention | 1 | `CHANGELOG.md` is the audit log; per-quarter review possible. |
| **O1** Release flow | 3 | `release.yml` with version validation, changelog generation (git-cliff), GitHub release via `softprops/action-gh-release` SHA-pinned. |
| **O2** Runbooks | 1 | `phenotype-infra/docs/runbooks/windows-desktop-runner.md` referenced in MEMORY but not in this repo. |
| **O3** On-call rotation | 0 | No evidence. |
| **O4** Dashboards | 1 | `docs/architecture/MONITORING_SECTIONS.md` exists; no live dashboard. |
| **O5** Alerts | 0 | No evidence. |
| **SC1** Dependency pinning | 2 | `package-lock.json` (in `open-sse/`); `open-sse/package.json:3.8.25` versioned; SHA pinning in workflows. |
| **SC2** SBOM | 1 | No SBOM in repo. `services/` registry on org has CycloneDX SBOMs (org-level). |
| **SC3** Attestation | 1 | No `attest-build-provenance` step (see S8). |
| **SC4** Provenance | 1 | No signed per-release provenance. |
| **OB1** Structured logs | 2 | CLAUDE.md:75 "log with pino context"; pino logger used (per evidence). |
| **OB2** Metrics | 1 | `Metron` repo + `phenoRouterMonitor` provide patterns; not in this repo. |
| **OB3** Traces | 1 | `phenotype-otel` exists at org; not wired here. |
| **OB4** SLOs | 0 | No evidence. |
| **C1** CI runner choice | 3 | `runs-on: ubuntu-24.04` / `ubuntu-latest` (no macOS/Windows runners). |
| **C2** Cache hit rate | 0 | No evidence. |
| **C3** Build time | 0 | No evidence. |
| **DA1** Schema migrations | 3 | `src/lib/db/migrations/` (CLAUDE.md:78 "versioned SQL files, idempotent, run in transactions"); 97+ migration files. |
| **DA2** Event versioning | 1 | `phenoEvents` repo (separate); pattern documented but not implemented here. |
| **DA3** API deprecation | 1 | OpenAPI present; no deprecation policy. |
| **RT1** MSRV / Node matrix | 2 | CLAUDE.md:138 "Node.js ≥22.0.0 <23 || ≥24.0.0 <27, ES Modules". |
| **RT2** Platform support | 1 | Linux + macOS mentioned (CLAUDE.md:139-140); no Windows evidence. |
| **RE1** Lockfile pinning | 2 | `package-lock.json`; SHA pinning for actions. |
| **RE2** Hermetic builds | 1 | `npm ci` implied; no explicit hermetic. |
| **AP1** OpenAPI current | 3 | `docs/reference/openapi.yaml` 178KB, ratchet-tracked. |
| **AP2** Contract tests | 1 | `tests/llm-security/`; protocol E2E. |
| **DM1** Rich domain | 2 | CLAUDE.md:42 "Domain/Policy: Policy engine, cost rules, fallback logic"; 3-layer resilience. |
| **DM2** Invariants in types | 2 | Circuit breaker states encoded as `CLOSED | OPEN | HALF_OPEN` (CLAUDE.md:55-66). |
| **EH1** Typed errors | 3 | CLAUDE.md:73 "try/catch with specific error types, log with pino context"; Zod at boundaries. |
| **EH2** Sanitization at boundary | 3 | CLAUDE.md:90-92 + `buildErrorBody()` / `sanitizeErrorMessage()`. |
| **CN1** No data races | 0 | No evidence (loom/ThreadSanitizer not seen). |
| **CN2** Async cancellation | 3 | CLAUDE.md:74 "Never swallow errors in SSE streams — use abort signals for cleanup". |
| **CN3** Idempotency | 0 | No evidence. |
| **PS1** Transaction boundaries | 2 | CLAUDE.md:78 "Migrations: `src/lib/db/migrations/` — versioned SQL files, idempotent, run in transactions". |
| **PS2** WAL / journaling | 2 | CLAUDE.md:78 "DB singleton: `getDbInstance()` from `src/lib/db/core.ts` (WAL journaling)". |
| **CF1** Env validation | 2 | CLAUDE.md:140 "Setup: `cp .env.example .env` then generate..."; `.env.example` 81KB. |
| **CF2** Secret zeroization | 2 | CLAUDE.md:89 "Encrypt credentials at rest (AES-256-GCM)". |
| **PR1** PII handling | 1 | CLAUDE.md:75 "Never swallow errors in SSE streams"; not PII-specific. |
| **PR2** Data retention | 0 | No evidence. |
| **RL1** Circuit breaker | 3 | `src/shared/utils/circuitBreaker.ts` (CLAUDE.md:53); 3-state; lazy recovery. |
| **RL2** Retry with backoff + jitter | 3 | `phenotype-retry` (org-level); CLAUDE.md:101-103 backoff formulas. |
| **RL3** Bulkhead | 1 | `connection cooldown` (CLAUDE.md:69-83) is per-connection isolation, partial. |
| **AS1** Loop detection | 0 | No evidence. |
| **AS2** Dry-run mode | 0 | No evidence. |
| **AU1** Lineage | 2 | `docs/operations/journey-traceability.md`; CHANGELOG per-PR. |
| **AU2** Decision records | 2 | 5 ADRs in `docs/adr/`. |

## Score distribution

- **3 (measured)**: 26 pillars
- **2 (wired)**: 26 pillars
- **1 (ad-hoc)**: 17 pillars
- **0 (absent)**: 12 pillars

**Mean score: 1.86 / 3.0** (62%)

## Strongest pillars (score 3)

X1 lint ratchet, X6 format, D4 changelog, D5 API ref, D6 arch map, T1-T3-T5-T6 testing, S1 SAST, S4 authn/authz, S5 input valid, S6 output sanit, S9 SHA pin, Q1 35 gates, Q2 ratchets, E4 co-author policy, E5 no bypass, G1 CODEOWNERS, O1 release flow, C1 runner choice, DA1 migrations, EH1 typed errors, EH2 sanitization, CN2 async cancel, RL1 circuit breaker, RL2 retry+jitter.

## Weakest pillars (score 0)

AT1 WCAG, AT2 keyboard, AT3 screen reader, P2 profiling, O3 on-call, O5 alerts, OB4 SLOs, C2 cache hit, C3 build time, CN1 data races, CN3 idempotency, PR2 retention, AS1 loop detect, AS2 dry-run.

## Top 10 quick wins (low effort, high impact)

1. **S8/SC3** Add `attest-build-provenance` step to `release.yml` (≤10 tool calls, unlocks SLSA L2)
2. **AT1/AT2/AT3** Add `axe-core` to E2E + keyboard test sweep (≤15 tool calls, opens A11y axis)
3. **P2** Add a single `cargo flamegraph` / `clinic.js` step in CI (≤10 tool calls, opens Perf axis)
4. **O3/O5** Wire SLOs to PagerDuty (≤5 tool calls)
5. **PR2** Document data retention schedule in `docs/privacy/RETENTION.md` (≤5 tool calls)
6. **AS1/AS2** Add `--dry-run` to release.yml + `--max-iter` to MCP tools (≤10 tool calls)
7. **C2/C3** Add cache hit + build time tracking to quality-baseline.json (≤5 tool calls)
8. **CN3** Add idempotency-key header handling to /api/v1 routes (≤20 tool calls)
9. **SC2** Run `cyclonedx-bom` in release.yml, upload SBOM artifact (≤10 tool calls)
10. **P4** Define 3 SLOs (availability, latency p99, error rate) in `docs/operations/SLOs.md` (≤5 tool calls)
